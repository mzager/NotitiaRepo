import { PlsConfigModel } from './pls.model';
import * as util from 'app/service/compute.worker.util';
import * as _ from 'lodash';

export const plsCompute = (config: PlsConfigModel): Promise<any> => {
    return null;
};
